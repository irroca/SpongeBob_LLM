# SpongeBob LLM dependencies
# NOTE: For a CPU-only environment (e.g. Cursor Cloud VMs without a GPU), install torch
# from the CPU wheel index first:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
# then: pip install -r requirements.txt

torch>=2.2
transformers>=4.40
tokenizers>=0.19
numpy>=1.26
scikit-learn>=1.3
tqdm>=4.66

# Optional: interactive chat colored output
colorama>=0.4

# Optional: experiment tracking (only needed when running with --use_wandb True)
# swanlab
